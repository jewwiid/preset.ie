{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "lib": ["deno.window"],
    "types": ["deno"]
  },
  "include": [
    "functions/**/*.ts"
  ],
  "exclude": [
    "node_modules",
    "dist"
  ]
}
